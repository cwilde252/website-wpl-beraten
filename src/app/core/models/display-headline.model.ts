/**
 * Eine Display-Headline als Daten. Zeilenumbrüche in einer großen Headline sind
 * Gestaltung und gehören deshalb in den Content, nicht ins Template.
 */
export interface DisplayHeadline {
  lines: string[];
}
